# 与 Telemail Worker 共享的密钥，两边必须一致
BRIDGE_SECRET=your-strong-random-secret

# Telemail Worker URL。Cloudflare Container 模式由 worker 自动注入内部 URL。
TELEMAIL_URL=https://your-worker.workers.dev

# 本地监听端口
PORT=3000
