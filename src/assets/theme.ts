/** 项目主题色（Tailwind slate/blue 色系） */
export const theme = {
  /** 主色（blue-500） */
  primary: "#3b82f6",
  /** 主色悬停（blue-600） */
  primaryHover: "#2563eb",
  /** 危险色（red-500） */
  danger: "#ef4444",
  /** 背景色（slate-900） */
  bg: "#0f172a",
  /** 卡片/面板背景（slate-800） */
  surface: "#1e293b",
  /** 边框色（slate-700） */
  border: "#334155",
  /** 正文色（slate-200） */
  text: "#e2e8f0",
  /** 次要文字（slate-400） */
  muted: "#94a3b8",
} as const;
